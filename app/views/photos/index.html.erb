<p id="notice"><%= notice %></p>

<h1>Photos</h1>

<table>
  <!-- <thead>
    <tr>
      <th>Image</th>
     <th>User</th>
      <th>Caption</th>
      <th colspan="3"></th>
    </tr>
  </thead>  -->

  <!-- <tbody>
    <% @photos.each do |photo| %>
      <tr>
        <td> <img src="<%= photo.image_url %>"> 
          <%= photo.caption %>
          <%= link_to like_photo_path(photo), method: :put do %>
            <i class="fa fa-thumbs-up"></i>
            <%= photo.get_upvotes.size %>
          <% end %>

          <%= link_to dislike_photo_path(photo), method: :put do %>
          <i class="fa fa-thumbs-down"></i>
            <%= photo.get_downvotes.size %>
          <% end %>
        </td>
        <td><%= link_to 'Show', photo %></td>
        <td><%= link_to 'Edit', edit_photo_path(photo) %></td>
        <td><%= link_to 'Destroy', photo, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
      <% if @photos.blank? %>
        <h4>There are no photos containing the term <%= params[:search] %>.</h4>
      <% end %>
    <% end %>
  </tbody>
</table> -->

<div class="container">
  <div class="row">
    <div class="col">
      <% @photos.each do |photo| %>
        <div class="block mb-5">
          <img src="<%= photo.image_url %>" class="img-fluid block">
          <p>
                      <%= link_to like_photo_path(photo), method: :put do %>
            <i class="fa fa-thumbs-up"></i>
            <%= photo.get_upvotes.size %>
          <% end %>
          <%= link_to 'Show', photo %>
          </p>
          <p>
            <%= photo.caption %>
          </p>
        </div>
      <% end %> 
    </div>
  </div>

<% if user_signed_in? %>
  <%= link_to 'New Photo', new_photo_path, class: "btn btn-success btn-lg" %>
<% end %>
